import {StyleSheet} from "react-native";

export const style = StyleSheet.create({
    container: {
        height: '100%',
        width: '100%',
        paddingLeft: 8,
        paddingRight: 8,
    },

});